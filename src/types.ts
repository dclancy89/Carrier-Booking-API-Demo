export enum UserType {
  CARRIER = 'carrier',
  CUSTOMER = 'customer,',
}

export enum AppointmentStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  ENROUTE = 'enroute',
  ARRIVED = 'arrived',
  COMPLETED = 'completed',
}
